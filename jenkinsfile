pipeline {
    agent any

    stages {

        stage('Run Program') {
            steps {
                sh 'python result.py'
            }
        }

        stage('Archive Report') {
            steps {
                archiveArtifacts artifacts: 'report.txt'
            }
        }
    }
}
